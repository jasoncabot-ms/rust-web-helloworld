name: Master

on:
  push:
    branches:
    - master

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Build a container image for ${GITHUB_REPOSITORY}
      run: docker build . -t rust-web-helloworld:${GITHUB_SHA}
      
